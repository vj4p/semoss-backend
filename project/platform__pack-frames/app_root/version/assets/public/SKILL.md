---
name: pack-frames
description: Use when you need to load a file into an in-memory frame, inspect its columns, and read rows back. Exposes these as tools you call directly: FileRead, CreateFrame, Import, FrameHeaders, FrameType, Collect.
---

# Frames pack

Load a file into an in-memory frame, inspect its columns, and read rows back.

These are tools **you** call during this run — not code to write into an app.

## Tools

- `FileRead`
- `CreateFrame`
- `Import`
- `FrameHeaders`
- `FrameType`
- `Collect`

Each tool's parameters and their descriptions come from the reactor itself, so read the tool schema rather than guessing argument names.
